export enum USER { 
    USERS = "users",
    USER = "user",
    ALREADY_VERIFIED = "already verified",
    NOT_VERIFIED = "not verified",
    NOT_EXIST = 'User account does not exist',
    WRONG_DATA = 'Wrong username or password'
}